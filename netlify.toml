# Netlify deployment config for admin-panel (React + Vite)

[build]
  command = "npm run build"
  publish = "dist"

# SPA routing — redirect all paths back to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
